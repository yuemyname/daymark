# WBS.md — daymark

작업 단위는 **세션(2~3시간, 저녁 한 번)** 이다. 시간 대신 세션을 쓰는 이유는 사이드 프로젝트에서 시간 추정이 항상 틀리기 때문이고, "오늘 하나 끝냈다"가 진도로 잡히는 편이 낫기 때문이다.

총 규모: **17세션** (버퍼 제외)

> 구 버전(20세션) 대비 변경: 아카이브 3세션이 통째로 빠지고(SPEC §11.4), 팔레트 작업이 톤 작업으로 줄고, 대신 성능·플리커 검증이 늘었다.

---

## 순서 원칙

1. **불확실한 것을 먼저 친다.** W0의 네 가지는 전부 "안 되면 설계를 바꿔야 하는" 항목이다.
2. **W2에서 한 번 세로로 관통한다.** 필드 1종 + 이펙트 1종만으로 시드→파라미터→필드→이펙트→시계→배포까지 뚫는다.
3. **결정성 테스트와 성능 테스트는 코드보다 먼저.** SPEC §12의 L1은 W1, L3은 W2에 들어간다. 매초 도는 렌더러에서 성능 테스트를 나중에 붙이면 이미 늦다.
4. **눈으로 보는 검수를 자르지 않는다.** 정지 그림이면 캡처 한 장으로 판단할 수 있지만, 이건 60초를 실제로 앉아서 봐야 판단할 수 있다.

---

## W0 — 리스크 스파이크 (3세션)

버리는 코드다. `spike/` 폴더에 짜고 끝나면 지운다. 목적은 "되는지 확인"이지 "구현"이 아니다.

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W0.1 | sfc32 수열 동일성 확인 | 스파이크 스크립트 | — | 0.5 | 같은 시드로 1,000개 뽑아 Chrome / Safari / Node 전부 일치 |
| W0.2 | **틱 렌더 성능 실측** | 이펙트 4종 × 필드 2종 벤치 | — | 1 | iPad Safari에서 720px 틱 총합 p95 ≤ 200ms. 초과하는 조합 목록화 |
| W0.3 | **플리커 검수** | 60초 연속 재생 페이지 | W0.2 | 1 | 계층 시드(SPEC §2.3)로 60초를 실제로 응시. 눈이 아프지 않을 것 |
| W0.4 | 바늘 가독성 | difference 합성 테스트 | — | 0.5 | 밀도 0.0/0.25/0.5/0.75/1.0 배경 전부에서 바늘이 보임 |

> **W0.3이 제일 중요하다.** 성능이 나와도 화면이 번쩍이면 이 프로젝트는 실패다. 여기서 계층 시드의 경계(무엇을 시/분/초에 붙일지)를 확정하고 넘어간다. SPEC §2.3의 표는 가설이지 결론이 아니다.
>
> W0.2에서 예산을 못 맞추는 조합이 나오면 **이펙트를 줄이지 말고 필드 해상도(512)를 먼저 의심할 것.** 대부분 필드가 아니라 이펙트의 마크 개수가 문제다.

---

## W1 — 결정성 코어 (3세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W1.1 | Vite + TS 스캐폴드, 폴더 구조 | 빈 프로젝트 | — | 0.5 | `npm run dev` 동작, 타입 에러 0 |
| W1.2 | ESLint 결정성 규칙 | `eslint.config.js` | W1.1 | 0.5 | `fields/`·`effects/`에 `Math.random`·`Date` 쓰면 빌드 실패 |
| W1.3 | `core/rng.ts` | cyrb128 + sfc32 | W0.1 | 0.5 | 유닛 테스트 통과 |
| W1.4 | `core/time.ts` + `core/clock.ts` | tsKey/hourKey/minuteKey, 드리프트 없는 틱 | W1.1 | 1 | TZ를 UTC로 바꿔도 결과 동일. 10분 연속 틱에서 초 경계 오차 ≤ 50ms |
| W1.5 | `core/tone.ts` | 전처리 체인 + paper/ink 모드 | W1.3 | 0.5 | 밀도 가드(0.30~0.55) 자동 보정 동작. 06:00/18:00 크로스페이드 |
| W1.6 | `params/` 골격 + 조합 선택 | `Params` 타입, 계층 추첨 | W1.3, W0.3 | 0.5 | 직전 2시간 이펙트 중복 회피. 궁합 가중치 반영 |
| W1.7 | **L1 결정성 테스트** | `determinism.test.ts` | W1.6 | 0.5 | 고정 타임스탬프 16개 params 해시 기록. `npm test` 통과 |

**Claude Code 첫 지시 예시**

> SPEC.md의 W1.1~W1.4를 진행해줘. §4 폴더 구조대로 스캐폴드하고, §2.1의 rng와 §2.2의 시각 유틸, §2.3의 계층 시드를 구현해. §2.4의 ESLint 규칙을 먼저 넣어서 이후 작업에서 강제되게 해줘. `Date`를 읽는 곳은 `core/clock.ts` 한 군데뿐이어야 해. 각 모듈에 유닛 테스트도 같이.

---

## W2 — 수직 슬라이스 (4세션)

필드 1종 + 이펙트 1종으로 끝까지 관통하고 **배포까지 한다.**

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W2.1 | `core/canvas.ts` + `sampler.ts` | DPR, 유닛 좌표, 이중선형 lum | W1.1 | 0.5 | §9의 스케일 방식을 하나로 확정하고 문서화 |
| W2.2 | F4 `subdivision` 구현 | 첫 필드 | W2.1, W1.6 | 0.5 | 분이 바뀌면 구도가 확실히 달라짐 |
| W2.3 | E1 `dots` 구현 | 첫 이펙트 | W2.2 | 1 | 하프톤 점 크기가 밀도를 따라감. 벤데이 격자 동작 |
| W2.4 | 캔버스 2장 + 틱 스케줄러 | 렌더 루프 | W2.3, W1.4 | 1 | 배경 1Hz / 바늘 60Hz. `document.hidden`에서 정지 |
| W2.5 | 시계 얼굴 렌더 | `clockface/draw.ts` | W2.4, W0.4 | 0.5 | difference + 여백 이중 보장. 눈금 12+60 |
| W2.6 | **L3 성능 테스트** + Netlify 배포 | 벤치 + 라이브 URL | W2.5 | 0.5 | 예산 초과 시 테스트 실패. iPad Safari에서 5분 연속 확인 |

> **왜 F4×E1을 먼저 하나:** 필드 쪽은 노이즈 라이브러리가 필요 없고 `phase`도 안 쓰는 유일한 정지 필드라, 파이프라인 문제와 애니메이션 문제가 섞이지 않는다. 이펙트 쪽은 하프톤이 "밀도 → 마크"의 가장 교과서적인 형태라 lum 샘플러가 맞는지 눈으로 바로 검증된다.
>
> W2.6의 "5분 연속"을 대충 하지 말 것. 30초는 어떤 구현이든 잘 돈다. 메모리 누수와 발열은 3분쯤부터 나온다.

---

## W3 — 필드·이펙트 확장 (5세션)

W2에서 확정된 `FieldFn` / `EffectFn` 인터페이스에 맞춰 추가만 한다. 서로 독립이라 **순서를 바꿔도 되고 하나씩 나눠서 해도 된다.** 다만 필드 하나 붙일 때마다 L3을 돌린다.

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W3.1 | F1 `noise` | 필드 | W2.2 | 0.5 | `phase`가 3번째 축. 1분 주기로 매끄럽게 순환 |
| W3.2 | **F2 `radial` + handInfluence** ★ | 필드 | W2.2 | 1 | 초·분·시침 각도가 밀도에 보임. 눈금과 주기 충돌 없음 |
| W3.3 | F3 `interference` | 필드 | W2.2 | 0.5 | 레이어 2~3개, 회색 죽이 되지 않음 |
| W3.4 | F5 `flow` | 필드 | W3.1 | 0.5 | 512 필드 생성 40ms 이내 |
| W3.5 | E2 `dither` | 이펙트 | W2.3 | 1 | Bayer 3종 + F-S. `pixelSize`가 출력 크기에 비례 (§9 예외) |
| W3.6 | E3 `stipple` | 이펙트 | W2.3 | 0.5 | 사각 그리드, 각도 회전 |
| W3.7 | E4 `steps` | 이펙트 | W2.3 | 0.5 | 계단 톤, rect/ellipse |
| W3.8 | 궁합 가중치 표 확정 | `params/index.ts` | W3.* | 0.5 | 20조합 전부 눈으로 확인, 못 쓸 조합은 가중치 0 |

> **W3.2가 이 프로젝트의 시그니처다.** 여유가 없으면 다른 걸 자르고 이건 남긴다. 배경이 그냥 예쁜 무늬가 아니라 "시각의 도해"가 되는 지점이 여기 하나뿐이다.
>
> E5~E8(edge, displace, automata, ascii)은 v1.1로 미룬다. 4종으로도 시(hour)마다 바뀌는 리듬은 성립한다. 특히 `automata`(SPEC §7.1)는 비용 모델이 다른 이펙트와 달라서, 나머지가 안정된 뒤에 붙이는 편이 안전하다.

---

## W4 — 튜닝 (3세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W4.1 | **60틱 미리보기 페이지** | 개발용 그리드 | W3.* | 0.5 | 1분치 60장 + 1시간치 60장을 한 화면에 |
| W4.2 | 파라미터 범위 튜닝 | 상수 조정 | W4.1 | 1 | 60장 중 "안 예쁜 틱"이 3장 이하. 24시간 전 구간 확인 |
| W4.3 | **L2 드로우 콜 트레이스** | 프록시 + 스냅샷 | W3.* | 1 | 필드 5 × 이펙트 4 트레이스 해시 기록 |
| W4.4 | 타이포/크롬 | 스타일 확정 | W2.5 | 0.5 | Fraunces + IBM Plex Mono. 크롬에 색 0 |

> **W4.1 → W4.2가 핵심 루프다.** 한 틱씩 보면서 튜닝하면 절대 못 맞춘다. 60장을 한 화면에 깔아야 "이 이펙트는 너무 자주 어둡다", "이 필드는 분 경계에서 튄다" 같은 게 보인다.
>
> 두 종류의 그리드가 필요하다. **1분치 60장**은 초 위상이 매끄러운지 보고, **1시간치 60장**(매 분 :00)은 분 경계 재추첨이 지나치게 튀지 않는지 본다. 둘은 다른 문제를 잡는다.
>
> 이 페이지는 프로덕션 라우트에 넣지 말고 `dev` 빌드에서만 노출.

---

## W5 — 상세와 내보내기 (2세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W5.1 | `/t/:timestamp` 판 상세 | plate 뷰 | W4.4 | 0.5 | 정지된 그림 + 파라미터 JSON + 시계 겹침 |
| W5.2 | PNG 내보내기 | `export/png.ts` | W5.1 | 1 | 4096px 산출물이 화면과 동일한 구도. 시계 포함/미포함 토글 |
| W5.3 | 저전력 모드 + 배포 마감 | — | W5.2 | 0.5 | 분 단위 갱신 토글, `prefers-reduced-motion` 연동 |

> W5.2가 SPEC §9(해상도 독립)의 **검수 지점**이다. 여기서 4096 결과가 화면과 다르면 어딘가에서 픽셀 상수를 썼다는 뜻이다. 디더링의 `pixelSize`가 제일 유력한 용의자다.

---

## 임계 경로

```
W0.2 → W0.3 → W1.6 → W1.7 → W2.3 → W2.4 → W2.6 → W4.1 → W4.2 → W5.1 → W5.2
```

W0.3(플리커)이 임계 경로의 맨 앞에 있다. 여기 결론이 §2.3의 계층 시드 표를 바꾸면 W1.6이 통째로 바뀌기 때문이다.

W0.1·W0.4는 임계 경로 밖이지만 **W0.4는 W2.5 전에 반드시** 끝나 있어야 한다.

W3의 여덟 항목은 서로 독립이라 임계 경로에 없다. 시간이 밀리면 여기서 자른다 — 필드 3종 + 이펙트 2종만 있어도 사이트는 성립한다.

---

## 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **화면이 번쩍여서 볼 수가 없음** | 치명적 — 시계로 못 씀 | W0.3에서 60초 응시 검수, §2.3 계층 시드, W4.1의 1분치 그리드 |
| **틱 예산 초과 / 발열** | 치명적 — 초침과 그림이 어긋남 | W0.2 실측, W2.6 L3 테스트, §13 스킵 규칙, `document.hidden` 정지 |
| 배경 밀도 때문에 바늘이 안 보임 | 높음 — 시계의 기능이 사라짐 | §10.1 이중 보장(difference + 여백), §8 밀도 가드 |
| 어딘가에서 `Math.random`/`Date` 유입 | 높음 — 조용히 깨지고 늦게 발견됨 | W1.2 린트 규칙 + W1.7 L1 테스트 |
| 이펙트 추가 시 과거 시각이 바뀜 | 중간 — 퍼머링크 신뢰도 붕괴 | §2.3 rng 분리(`:pick`/`:config`) + L1 스냅샷 |
| 4096 내보내기가 화면과 다름 | 중간 — 뒤늦게 전 이펙트 수정 | W2.1에서 스케일 방식 확정, §9의 디더링 예외를 주석으로 명시 |
| 밤에 흰 화면 | 중간 — 실사용 불가 | §8 모드 고정(추첨 아님) |

---

## 자를 수 있는 것 / 없는 것

**자른다:** W3의 필드 2종·이펙트 2종, W5.2의 4096(2048까지만), W5.3 저전력 토글(`prefers-reduced-motion` 대응만 남김)

**안 자른다:** W0.3 플리커 검수, W1.2 린트, W1.7 L1, W2.6 L3, W4.1~W4.2 튜닝 루프, W3.2 handInfluence

W4.2를 자르고 싶은 유혹이 제일 크다. 코드는 다 돌아가고 화면도 나오니까 끝난 것 같기 때문이다. 하지만 매초 도는 화면에서 파라미터 범위가 안 맞으면, 정지 그림보다 **60배 빨리** 지겨워지거나 60배 빨리 피곤해진다. 여기를 건너뛰면 켜놓고 싶지 않은 시계가 된다.
