# WBS.md — daymark

작업 단위는 **세션(2~3시간, 저녁 한 번)** 이다. 시간 대신 세션을 쓰는 이유는 사이드 프로젝트에서 시간 추정이 항상 틀리기 때문이고, "오늘 하나 끝냈다"가 진도로 잡히는 편이 낫기 때문이다.

총 규모: **20세션** (버퍼 제외)

---

## 순서 원칙

1. **불확실한 것을 먼저 친다.** W0의 세 가지는 전부 "안 되면 설계를 바꿔야 하는" 항목이다. 이걸 W3쯤에서 발견하면 앞의 작업을 버리게 된다.
2. **W2에서 한 번 세로로 관통한다.** 시스템 1종만으로 시드→파라미터→렌더→화면→배포까지 끝까지 뚫는다. 시스템 5종을 먼저 다 만들고 화면을 붙이면, 화면 요구사항 때문에 시스템 인터페이스를 다시 고치게 된다.
3. **결정성 테스트는 코드보다 먼저.** SPEC §10의 L1은 W1에 들어간다. 나중에 붙이면 이미 깨진 뒤라 기준선을 잡을 수 없다.

---

## W0 — 리스크 스파이크 (2세션)

버리는 코드다. `spike/` 폴더에 짜고 끝나면 지운다. 목적은 "되는지 확인"이지 "구현"이 아니다.

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W0.1 | sfc32 수열 동일성 확인 | 스파이크 스크립트 | — | 0.5 | 같은 시드로 1,000개 뽑아 Chrome / Safari / Node에서 전부 일치 |
| W0.2 | OKLCH → sRGB 색역 클리핑 확인 | 팔레트 100개 미리보기 페이지 | — | 1 | 100개 중 대비 미달(ΔL<0.35)이 0개. 색이 뭉개지는 케이스 파악 |
| W0.3 | 아카이브 렌더 부하 측정 | 31칸 썸네일 벤치 | — | 0.5 | 128px 31칸 렌더 시간 측정. iPad에서 프레임 드롭 확인 |

> **W0.2가 제일 중요하다.** 색이 안 예쁘면 나머지가 다 잘 돌아가도 이 프로젝트는 실패다. 여기서 팔레트 100개를 눈으로 보고 §7 규칙을 조정하고 넘어간다.

---

## W1 — 결정성 코어 (4세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W1.1 | Vite + TS 스캐폴드, 폴더 구조 | 빈 프로젝트 | — | 0.5 | `npm run dev` 동작, 타입 에러 0 |
| W1.2 | ESLint 결정성 규칙 | `eslint.config.js` | W1.1 | 0.5 | `systems/`에 `Math.random` 쓰면 빌드 실패 |
| W1.3 | `core/rng.ts` | cyrb128 + sfc32 | W0.1 | 0.5 | 유닛 테스트 통과 |
| W1.4 | `core/date.ts` | todayKey, 자정까지 남은 시간 | W1.1 | 0.5 | 타임존을 UTC로 바꿔도 결과 동일. 윤일·연말 케이스 통과 |
| W1.5 | `core/palette.ts` | 팔레트 생성기 | W0.2, W1.3 | 1 | ΔL 가드 통과. 대비 미달 시 자동 보정 |
| W1.6 | `params/` 골격 + 시스템 선택 규칙 | `Params` 타입, 추첨 로직 | W1.3 | 0.5 | 직전 2일 중복 회피 동작 확인 |
| W1.7 | **L1 결정성 테스트** | `determinism.test.ts` | W1.6 | 0.5 | 고정 날짜 12개의 params 해시 기록. `npm test` 통과 |

**Claude Code 첫 지시 예시**

> SPEC.md의 W1.1~W1.4를 진행해줘. §4 폴더 구조대로 스캐폴드하고, §2.1의 rng와 §2.2의 date 유틸을 구현해. §2.3의 ESLint 규칙을 먼저 넣어서 이후 작업에서 강제되게 해줘. 각 모듈에 유닛 테스트도 같이.

---

## W2 — 수직 슬라이스 (4세션)

시스템 1종으로 끝까지 관통하고 **배포까지 한다.**

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W2.1 | `core/canvas.ts` — DPR, 유닛 좌표 | 캔버스 헬퍼 | W1.1 | 0.5 | §8의 스케일 방식을 하나로 확정하고 문서화 |
| W2.2 | S4 `subdivision` 구현 | 첫 시스템 | W2.1, W1.6 | 1 | 날짜 바꾸면 구도가 확실히 달라짐 |
| W2.3 | `/` 오늘 화면 (정적) | today 뷰 | W2.2 | 1 | 그림 + 판 캡션 + 카운트다운 |
| W2.4 | 점진 렌더 (rAF 청크) | 렌더 스케줄러 | W2.3 | 0.5 | 그려지는 과정이 보임. 메인 스레드 16ms 초과 없음 |
| W2.5 | Netlify 배포 | 라이브 URL | W2.3 | 0.5 | iPad Safari에서 확인 |

> **왜 S4(재귀 분할)를 먼저 하나:** 5종 중 제일 단순하고 노이즈 라이브러리가 필요 없어서, 파이프라인 문제와 알고리즘 문제가 섞이지 않는다. 여기서 뭔가 이상하면 원인은 무조건 파이프라인이다.

---

## W3 — 시스템 4종 (5세션)

W2에서 확정된 `RenderFn` 인터페이스에 맞춰 추가만 한다. 각 시스템은 서로 독립이라 **순서를 바꿔도 되고, 하나씩 나눠서 해도 된다.**

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W3.1 | S3 `truchet` | 시스템 | W2.2 | 1 | 재귀 분할 타일 동작, 굵기가 스케일에 비례 |
| W3.2 | S1 `flowfield` | 시스템 | W2.2 | 1.5 | 입자 1,000개 렌더 2초 이내 |
| W3.3 | S5 `interference` | 시스템 | W2.2 | 1 | 레이어 2~3개, 회색 죽이 되지 않음 |
| W3.4 | S2 `packing` (마스크 제외) | 시스템 | W2.2 | 1 | 공간 격자 충돌 검사, 원 2,000개 부드럽게 |
| W3.5 | S2 `dateGlyph` 마스크 | 마스크 모드 | W3.4 | 0.5 | 일(日) 숫자 안에만 원이 채워짐 |

> W3.5가 이 프로젝트의 **시그니처**다. 여유가 없으면 다른 걸 자르고 이건 남긴다.

---

## W4 — 다듬기 (3세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W4.1 | **L2 드로우 콜 트레이스 테스트** | 프록시 + 스냅샷 | W3.* | 1 | 5종 전부 트레이스 해시 기록 |
| W4.2 | 90일치 일괄 미리보기 페이지 | 개발용 그리드 | W3.* | 0.5 | 90장 한눈에 보기 |
| W4.3 | 파라미터 범위 튜닝 | 상수 조정 | W4.2 | 1 | 90장 중 "안 예쁜 날"이 5장 이하 |
| W4.4 | 타이포/크롬 디자인 | 스타일 확정 | W2.3 | 0.5 | Fraunces + IBM Plex Mono, accent 연동 |

> **W4.2 → W4.3이 핵심 루프다.** 한 장씩 보면서 튜닝하면 절대 못 맞춘다. 90장을 한 화면에 깔아놓고 봐야 "이 시스템은 너무 자주 어둡다" 같은 게 보인다. 이 페이지는 프로덕션 라우트에 넣지 말고 `dev` 빌드에서만 노출.

---

## W5 — 아카이브 (3세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W5.1 | 달력 그리드 레이아웃 | archive 뷰 | W4.4 | 1 | 월 단위 7열, 미래 날짜는 빈 칸 |
| W5.2 | 렌더 큐 + IntersectionObserver | 지연 렌더 | W5.1, W0.3 | 1 | 프레임당 1~2칸. 스크롤 중 끊김 없음 |
| W5.3 | `ImageBitmap` 캐시 | 캐시 레이어 | W5.2 | 0.5 | 재방문한 달이 즉시 표시 |
| W5.4 | 월 이동 네비게이션 | 라우팅 | W5.1 | 0.5 | URL에 월이 반영됨 |

---

## W6 — 상세와 내보내기 (2세션)

| ID | 작업 | 산출물 | 선행 | 규모 | 완료 기준 |
|---|---|---|---|---|---|
| W6.1 | `/d/:date` 판 상세 | plate 뷰 | W5.4 | 0.5 | 큰 그림 + 파라미터 JSON |
| W6.2 | PNG 내보내기 | `export/png.ts` | W6.1 | 1 | 4096px 산출물이 화면과 동일한 구도 |
| W6.3 | 진행 표시 + 배포 마감 | — | W6.2 | 0.5 | 4096 렌더 중 UI가 멈추지 않음 |

> W6.2가 §8(해상도 독립)의 **검수 지점**이다. 여기서 4096 결과가 화면과 다르면, 어딘가에서 픽셀 상수를 썼다는 뜻이다. W2~W3을 대충 했으면 여기서 값을 치른다.

---

## 임계 경로

```
W0.1 → W1.3 → W1.6 → W1.7 → W2.2 → W2.3 → W4.2 → W4.3 → W5.1 → W5.2 → W6.1 → W6.2
```

W0.2(팔레트)와 W0.3(성능 측정)은 임계 경로 밖이라 언제 해도 되지만, **W0.2는 W1.5 전에 반드시** 끝나 있어야 한다.

W3의 다섯 항목은 서로 독립이라 임계 경로에 없다. 시간이 밀리면 여기서 자른다 — 시스템 3종만 있어도 사이트는 성립한다.

---

## 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 팔레트가 안 예쁜 날이 자주 나옴 | 치명적 — 프로젝트의 존재 이유가 사라짐 | W0.2에서 100개 눈으로 검수, W4.3에서 90일치 재검수 |
| 어딘가에서 `Math.random` 유입 | 치명적 — 조용히 깨지고 늦게 발견됨 | W1.2 린트 규칙 + W1.7 L1 테스트 |
| 시스템 추가 시 과거 날짜가 바뀜 | 높음 — 아카이브 신뢰도 붕괴 | SPEC §5의 rng 분리(`:params` / `:render`) + L1 스냅샷 |
| 아카이브가 iPad에서 버벅임 | 중간 | W0.3에서 미리 측정, W5.2 렌더 큐 |
| 4096 내보내기가 화면과 다름 | 중간 — 뒤늦게 전 시스템 수정 | W2.1에서 스케일 방식을 하나로 확정하고 5종 전부 통일 |

---

## 자를 수 있는 것 / 없는 것

시간이 밀렸을 때의 판단 기준을 미리 정해둔다.

**자른다:** W3의 시스템 2종, W5.3 캐시, W5.4 월 이동, W6.2 4096(2048까지만)
**안 자른다:** W1.2 린트, W1.7 L1 테스트, W4.2~W4.3 튜닝 루프, W3.5 dateGlyph

W4.3을 자르고 싶은 유혹이 제일 크다. 코드는 다 돌아가니까 끝난 것 같기 때문이다. 하지만 여기를 건너뛰면 매일 열어보고 싶지 않은 사이트가 된다.
